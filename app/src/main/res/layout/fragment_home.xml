<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/home_layout"
    android:layout_marginTop="15sp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Header -->
    <TextView
        android:id="@+id/home_title"
        android:text="@string/app_name"
        android:textSize="25sp"
        android:textStyle="bold"
        android:layout_marginStart="15sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/home_notification"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:tint="@color/black"
        app:srcCompat="@drawable/home_notification"
        android:layout_marginEnd="15dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <!-- End Header -->
    <!-- Story Bar -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/home_storyBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toBottomOf="@+id/home_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
    <com.google.android.material.divider.MaterialDivider
        android:id="@+id/home_divUnderstoryBar"
        android:layout_width="0dp"
        android:layout_height="2dp"
        app:dividerColor="@color/disabledColorA30"
        app:layout_constraintTop_toBottomOf="@+id/home_storyBar"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <!-- End Story Bar -->
    <!-- Posts -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/home_posts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/home_divUnderstoryBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <!-- End Posts -->


</androidx.constraintlayout.widget.ConstraintLayout>